<analysis>**original_problem_statement:** The user wants to build a full-stack Event Management Admin System.

**PRODUCT REQUIREMENTS:**
- **Technology Stack**:
    - **Frontend**: React 18, Vite, TypeScript, , , Tailwind CSS, . The UI is based on a Figma-generated project.
    - **Backend**: Node.js, TypeScript, Express.js, PostgreSQL, Knex (as query builder).
    - **Architecture**: Strict layered architecture (routes, controllers, services, DTOs).
- **Modules**:
    1.  **Authentication**: JWT-based login, logout, and token validation.
    2.  **Events Overview**: A dashboard to view, filter, sort, and paginate events, with metric cards and bulk actions.
    3.  **Create Event**: A form to add new events with file uploads to S3, virtual integrations (Zoom/Meet), and SEO auto-generation.
    4.  **Event Detail Page**: A multi-tab view for managing a single event, including an Overview tab, a Tickets tab (with sub-tabs for inventory, add-ons, promos, and settings), and other future tabs.

**User's preferred language**: English

**what currently exists?**
A full-stack application with a Node.js/Express/TypeScript backend using PostgreSQL and Knex, and a React/Vite/TypeScript frontend. The environment has been stabilized after initial issues with supervisor configurations and an erroneous migration to MongoDB which was fully reverted.

- **Authentication**: Complete and functional.
- **Create Event**: The form is functional, including the mandatory file upload feature which was implemented and tested. It uses a local file-based mock for S3.
- **Events Overview**: The main event listing page is complete, fetching and displaying data from the backend.
- **Event Detail - Overview Tab**: Fully implemented and tested. It displays live data for metrics, registration funnel, alerts, and recent activity.
- **Event Detail - Tickets Tab (Inventory)**: Fully implemented and tested. It provides full CRUD functionality for event tickets, with the UI correctly wired to the backend.

**Last working item**:
-   Last item agent was working: Implementing the sub-tabs within the Event Detail -
